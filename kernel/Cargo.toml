[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
limine = "0.5.0"
libm = "0.2.16"

[lib]
crate-type = ["staticlib"]
path = "main.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
